{
  "name": "fargate-inter-container-networking",
  "version": "0.0.1",
  "description": "shows how two containers running in a single fargate task can communicate over the local loopback interface",
  "main": "index.js",
  "scripts": {
    "build": "tsc --build tsconfig.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^14.0.0",
    "aws-cdk-lib": "^2.50.0",
    "constructs": "^10.0.0",
    "ts-node": "^10.1.0",
    "typescript": "^4.6.0"
  }
}
